name: Remove Boost + Fast Pool (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired one-shot migration workflow; no repository mutation."
