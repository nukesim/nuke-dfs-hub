name: Fix FanDuel Export UI
on:
  push:
    paths:
      - 'tools/fix_fanduel_export_ui.py'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python tools/fix_fanduel_export_ui.py
      - run: python -m py_compile pages/6_SIM.py fd_export.py dfs_export.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pages/6_SIM.py
          git diff --cached --quiet || git commit -m "Fix FanDuel export IDs and platform labels"
          git push
