name: Lazy Pool + Candidate Diagnostics
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lazy pool and candidate diagnostics migration completed."
