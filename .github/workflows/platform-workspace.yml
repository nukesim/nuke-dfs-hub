name: Platform Workspace Persistence
on:
  push:
    paths:
      - 'tools/platform_workspace_patch.py'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python tools/platform_workspace_patch.py
      - run: python -m py_compile pages/6_SIM.py nuke_platform_workspace.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pages/6_SIM.py
          git diff --cached --quiet || git commit -m "Persist separate DraftKings and FanDuel SIM workspaces"
          git push
